/* ============================================================
   XELON AERO — настройки сайта (редактируются без пересборки)
   ============================================================ */
window.XELON_CONFIG = {
  /* Formspree: создайте форму на https://formspree.io и вставьте её ID.
     Пока здесь стоит заглушка, форма работает в демо-режиме и заявки не отправляются. */
  formEndpoint: "https://formspree.io/f/YOUR_FORM_ID",

  /* Альтернатива: свой обработчик/CRM-вебхук (POST, JSON). Если указан, используется вместо Formspree. */
  crmWebhook: "",

  /* Отображение цен в юанях (RMB).
     null — переключатель ₽/¥ скрыт. Укажите курс (сколько рублей за 1 юань),
     чтобы показать переключатель; пересчёт помечается как ориентировочный. */
  rubPerCny: null
};
